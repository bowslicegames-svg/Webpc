name: Upload disk.raw

on:
  workflow_dispatch:

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - name: Upload disk.raw
        uses: actions/upload-artifact@v4
        with:
          name: disk-raw-upload
          path: disk.raw
