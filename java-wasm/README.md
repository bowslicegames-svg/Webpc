# java-wasm

Placeholders for Java→WASM tooling and a launcher shim. Real work: AOT compile JVM or use CheerpJ/GraalVM/TeaVM.

This folder contains a small launcher shim that demonstrates how the native launcher could be wrapped.
