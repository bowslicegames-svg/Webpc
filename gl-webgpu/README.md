# gl-webgpu

Contains a tiny translator stub and a browser-side renderer. The translator converts intercepted GL calls into a compact command stream; the renderer executes them with WebGPU/WebGL.

This folder contains minimal JS stubs to illustrate the flow.
